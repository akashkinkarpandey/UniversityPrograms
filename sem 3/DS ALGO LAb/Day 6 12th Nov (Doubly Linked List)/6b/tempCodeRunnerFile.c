int c = count();
            // if (f2 == -1)
            //     printf("No such node found\n");
            // else if (f2 == c)
            //     printf("No node after %d node\n", t2);
            // else
            //     deleteFromAnyPosition(f2 + 1);